<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间应该是对应接口的包名+接口名 -->
<mapper namespace="com.dcseat.report.dao.seat.Paps">
    <!--id应该是接口中的方法，结果类型如没有配置别名则应该使用全名称 -->
    <select id="getPapsByCorp"  resultType="java.lang.Float">
        SELECT
            SUM( pap.`value` )
        FROM
            kassie_calendar_paps pap
            JOIN corporation_members cm ON pap.character_id = cm.character_id
        WHERE
            cm.corporation_id = #{corpId}
            AND pap.YEAR = #{year}
            AND pap.`month` = #{month};
    </select>

</mapper>